mainSections       = ["post"]
description        = "Hitchhiking through raw bits"

[sidebar]
    subtitle = "Hitchhiking through raw bits"

[widgets]
    homepage = [
        { type = "about" },
        { type = "categories", position = "bot" },
        { type = "tags", position = "bot" },
    ]
    page = [
        { type = "about" },
        { type = "toc" },
    ]

    [widgets.about]
        title       = "about"
        description = "Engineering notes from Przemek. Focused on performance, the command line, and how the web actually moves bytes."
